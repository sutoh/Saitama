<h1>Listing work_skills</h1>

<table>
  <tr>
    <th>Work detail</th>
    <th>Skill</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @work_skills.each do |work_skill| %>
  <tr>
    <td><%= work_skill.work_detail_id %></td>
    <td><%= work_skill.skill_id %></td>
    <td><%= link_to 'Show', work_work_detail_work_skill_path(params[:work_id], params[:work_detail_id], work_skill) %></td>
    <td><%= link_to 'Edit', edit_work_work_detail_work_skill_path(params[:work_id], params[:work_detail_id], work_skill) %></td>
    <td><%= link_to 'Destroy', work_work_detail_work_skill_path(params[:work_id], params[:work_detail_id], work_skill), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Work skill', new_work_work_detail_work_skill_path(params[:work_id], params[:work_detail_id]) %>
