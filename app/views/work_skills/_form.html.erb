<%= form_for([@work, @work_detail, @work_skill]) do |f| %>
  <% if @work_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_skill.errors.count, "error") %> prohibited this work_skill from being saved:</h2>

      <ul>
      <% @work_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_detail_id %><br />
    <%= f.number_field :work_detail_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br />
    <%= f.number_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
