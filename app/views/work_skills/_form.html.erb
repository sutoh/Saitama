<%= form_for([@work, @work_detail, @work_skill], :html => {:class => "form-horizontal"}) do |f| %>
  <% if @work_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_skill.errors.count, "error") %> prohibited this work_skill from being saved:</h2>

      <ul>
      <% @work_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field control-group">
    <%= f.label :work_detail_id, :class => "control-label" %>
    <div class="controls">
      <%= f.number_field :work_detail_id %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :skill_id, :class => "control-label" %>
    <div class="controls">
      <%= f.number_field :skill_id %>
    </div>
  </div>
  <div class="actions control-group">
    <div class="controls">
      <%= f.submit :class => "btn btn-primary"%>
    </div>
  </div>
<% end %>
